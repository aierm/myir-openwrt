#!/bin/sh
. /lib/functions/uci-defaults.sh

board=$(board_name)

board_config_update

case "$board" in
myir,mys-rzg2l-wifi)
	# No user LEDs are wired up in the current DTS.
	;;
esac

board_config_flush

exit 0
